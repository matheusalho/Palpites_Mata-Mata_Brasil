# Copie para .env.local e preencha com a URL do gatilho HTTP do fluxo do Power Automate.
# (.env.local é ignorado pelo git; no GitHub Pages a URL vem do Secret VITE_ENDPOINT_PALPITES.)
VITE_ENDPOINT_PALPITES=
